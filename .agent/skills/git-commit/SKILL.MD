# Git Auto Commit Skill

## Description
Automatically:
1. git add .
2. git commit
3. git push current branch

## Parameters
- message (optional): custom commit message

## Behavior
- If message is provided → use it
- If not → auto generate commit message
- If no changes → skip commit